import os
import random

from controller.webscraper import WebScraper
from log.logger import Logger
from proxy.proxy_handler import ProxyHandler

if __name__ == '__main__':

    # h = ProxyHandler()
    # h.process_proxies()
    #
    # cwd = os.getcwd()
    # # parent_dir = "Szakdolgozat_gwjz4t"
    # parent_dir = "dblin"
    # # while not cwd.endswith(parent_dir):
    # #     print(cwd)
    # #
    # #     cwd = os.path.dirname(cwd)
    # #
    # # cwd.
    # result = os.getcwd().rsplit(parent_dir, 1)[0] + parent_dir


    l = Logger()
    l.info("puppyfarts")

    # url = "https://pythonprogramming.net/parsememcparseface/"
    # s = WebScraper()
    # r = s.request_sauce(url, "text")
    # print(r)

